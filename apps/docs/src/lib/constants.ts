import { env } from "@/env";

/** Landing site URL (used for "Back to landing" navigation). */
export const LANDING_URL = env.NEXT_PUBLIC_LANDING_URL;

/** GitHub repository URL. */
export const GITHUB_URL = env.NEXT_PUBLIC_GITHUB_URL;

/** Canonical site URL for the docs site (used for metadata, sitemaps, hreflang). */
export const SITE_URL = env.NEXT_PUBLIC_SITE_URL;
