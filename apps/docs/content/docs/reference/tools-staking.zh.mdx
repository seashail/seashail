---
title: "工具：质押"
description: 通过 Lido、Eigenlayer、Marinade 和 Jito 质押和取消质押代币。
---

质押工具管理流动质押仓位。所有工具需要[策略审批](/zh/docs/reference/tools-policy)。省略 protocol 时按链自动选择（EVM 默认 Lido，Solana 默认 Jito）。

## stake_tokens

质押代币以赚取质押奖励。Lido（Ethereum）和 Jito（Solana）支持原生质押路径；其他协议通过交易封装回退执行。

### 参数

<TypeTable
  type={{
    wallet: {
      type: "string",
      description: "钱包名称。若省略，使用活跃钱包。",
      default: "active wallet",
    },
    account_index: {
      type: "integer",
      description: "钱包内的账户索引。若省略，使用活跃账户索引。",
      default: "active index",
    },
    chain: {
      type: "string",
      description:
        '执行的链："ethereum" 或 "solana"。其他 EVM 链需要交易封装回退。',
      required: true,
    },
    protocol: {
      type: '"lido" | "eigenlayer" | "marinade" | "jito"',
      description:
        "协议选择。若省略，按链默认：EVM 默认 Lido，Solana 默认 Jito。",
    },
    token: {
      type: "string",
      description:
        '要质押的代币。原生质押使用 "native"（Lido 对应 ETH，Jito 对应 SOL）。',
      required: true,
    },
    amount: {
      type: "string",
      description: "以十进制字符串表示的质押金额。原生质押路径必填。",
      required: true,
    },
    amount_units: {
      type: '"ui" | "base"',
      description:
        "ui = 人类可读代币单位（ETH、SOL）。base = 原始单位（wei、lamports）。",
      default: '"ui"',
    },
    slippage_bps: {
      type: "integer",
      description:
        "Jito 质押的滑点容忍度，单位基点（1 基点 = 0.01%）（使用 Jupiter 兑换）。Lido 不适用。范围：0–2000。",
      default: "100",
    },
    usd_value: {
      type: "number",
      description: "用于策略执行的尽力美元价值。若省略，视为未知。",
    },
    usd_value_known: {
      type: "boolean",
      description:
        "表示 usd_value 是否已知的覆盖标志。若提供了 usd_value，则视为 true。",
      default: "false",
    },
    to: {
      type: "string",
      description: "EVM：交易接收方/合约地址。用于交易封装回退模式。",
    },
    data: {
      type: "string",
      description: "EVM：0x 前缀的调用数据。空时使用 0x。",
    },
    value_wei: {
      type: "string",
      description: "EVM：以十进制字符串表示的 wei 价值。",
      default: '"0"',
    },
    tx_b64: {
      type: "string",
      description:
        "Solana：base64 编码的 VersionedTransaction 字节（未签名；Seashail 将签名）。",
    },
    allowed_program_ids: {
      type: "string[]",
      description: "Solana：程序 ID 白名单。每条指令的程序 ID 必须在此列表中。",
    },
    asset: {
      type: "object",
      description: "协议特定请求对象，供可选适配器用于构建交易封装。",
    },
  }}
/>

### 各协议行为

<Tabs
  items={[
    "Lido (EVM)",
    "Eigenlayer (EVM)",
    "Jito (Solana)",
    "Marinade (Solana)",
  ]}
>
  <Tab value="Lido (EVM)">
    - 省略 `protocol` 时 EVM 链的默认协议 - 质押：通过 Lido 质押合约将原生 ETH
    兑换为 stETH - 代币标识符：`"native"`（ETH）- 原生执行路径：以 ETH 价值调用
    Lido `submit(address _referral)` - 返回交易哈希（`txid`）和美元价值 - 支持
    Ethereum 主网
  </Tab>
  <Tab value="Eigenlayer (EVM)">
    - 显式设置 `protocol: "eigenlayer"` - 通过交易封装回退执行 -
    需要智能体或适配器提供封装字段（`to`、`data`、`value_wei`）
  </Tab>
  <Tab value="Jito (Solana)">
    - 省略 `protocol` 时 Solana 的默认协议 - 质押：通过 Jupiter 兑换将原生 SOL
    兑换为 JitoSOL - 代币标识符：`"native"`（SOL）- 原生执行路径：在内部使用
    Jupiter 兑换聚合器 - `slippage_bps` 控制兑换滑点容忍度 - 返回签名和美元价值
    - 支持 Solana 主网
  </Tab>
  <Tab value="Marinade (Solana)">
    - 显式设置 `protocol: "marinade"` - 通过交易封装回退执行 -
    需要智能体或适配器提供封装字段（`tx_b64`、`allowed_program_ids`）
  </Tab>
</Tabs>

### 响应

```json
{
  "chain": "solana",
  "protocol": "jito",
  "signature": "5UfD...txid",
  "usd_value": 42.5
}
```

响应结构仅供参考，实际字段可能因协议而异。Lido 返回 `txid` 而非 `signature`。

### 示例

<Tabs items={["Lido (Ethereum)", "Jito (Solana)"]}>
  <Tab value="Lido (Ethereum)">
    ```json
    {
      "chain": "ethereum",
      "protocol": "lido",
      "token": "native",
      "amount": "0.1",
      "amount_units": "ui"
    }
    ```
  </Tab>
  <Tab value="Jito (Solana)">
    ```json
    {
      "chain": "solana",
      "protocol": "jito",
      "token": "native",
      "amount": "1.5",
      "amount_units": "ui",
      "slippage_bps": 100
    }
    ```
  </Tab>
</Tabs>

### 注意事项

- 需要[策略审批](/zh/docs/reference/tools-policy)——交易可能被自动批准、需要确认或被阻止，取决于策略配置和美元价值。
- **协议自动选择：** 若省略 `protocol`，Seashail 按链选择（EVM → Lido，Solana → Jito）。
- **Jito 兑换机制：** Solana 上的 Jito 质押使用 Jupiter 兑换将 SOL 转换为 JitoSOL。`slippage_bps` 控制此兑换的最大滑点容忍度。
- **原生执行路径：** 提供 `token`、`amount` 和 `chain` 时，Lido（Ethereum ETH）和 Jito（Solana SOL）有原生执行路径。其他协议回退到交易封装模式。
- **交易封装回退：** 如果原生执行不可用或提供了封装字段，Seashail 执行提供的交易封装。
- 另见：[DeFi 指南](/zh/docs/guides/defi)

---

## unstake_tokens

取消质押代币并赎回质押奖励。Lido（Ethereum，创建提款请求）和 Jito（Solana，兑换回 SOL）支持原生取消质押路径；其他协议通过交易封装回退执行。

### 参数

<TypeTable
  type={{
    wallet: {
      type: "string",
      description: "钱包名称。若省略，使用活跃钱包。",
      default: "active wallet",
    },
    account_index: {
      type: "integer",
      description: "钱包内的账户索引。若省略，使用活跃账户索引。",
      default: "active index",
    },
    chain: {
      type: "string",
      description:
        '执行的链："ethereum" 或 "solana"。其他 EVM 链需要交易封装回退。',
      required: true,
    },
    protocol: {
      type: '"lido" | "eigenlayer" | "marinade" | "jito"',
      description:
        "协议选择。若省略，按链默认：EVM 默认 Lido，Solana 默认 Jito。",
    },
    token: {
      type: "string",
      description:
        "要取消质押的代币。原生取消质押：stETH（Lido，默认）或 JitoSOL（Jito，默认）。",
      required: true,
    },
    amount: {
      type: "string",
      description: "以十进制字符串表示的取消质押金额。原生取消质押路径必填。",
      required: true,
    },
    amount_units: {
      type: '"ui" | "base"',
      description:
        "ui = 人类可读代币单位（stETH、JitoSOL）。base = 原始单位（wei、lamports）。",
      default: '"ui"',
    },
    request_id: {
      type: "string",
      description:
        "可选异步请求 ID，用于恢复/认领（未来支持）。目前原生 Lido 取消质押返回 request_ids，但认领功能尚未实现。",
    },
    slippage_bps: {
      type: "integer",
      description:
        "Jito 取消质押的滑点容忍度，单位基点（1 基点 = 0.01%）（使用 Jupiter 兑换）。Lido 不适用。范围：0–2000。",
      default: "100",
    },
    usd_value: {
      type: "number",
      description: "用于策略执行的尽力美元价值。若省略，视为未知。",
    },
    usd_value_known: {
      type: "boolean",
      description:
        "表示 usd_value 是否已知的覆盖标志。若提供了 usd_value，则视为 true。",
      default: "false",
    },
    to: {
      type: "string",
      description: "EVM：交易接收方/合约地址。用于交易封装回退模式。",
    },
    data: {
      type: "string",
      description: "EVM：0x 前缀的调用数据。空时使用 0x。",
    },
    value_wei: {
      type: "string",
      description: "EVM：以十进制字符串表示的 wei 价值。",
      default: '"0"',
    },
    tx_b64: {
      type: "string",
      description:
        "Solana：base64 编码的 VersionedTransaction 字节（未签名；Seashail 将签名）。",
    },
    allowed_program_ids: {
      type: "string[]",
      description: "Solana：程序 ID 白名单。每条指令的程序 ID 必须在此列表中。",
    },
    asset: {
      type: "object",
      description: "协议特定请求对象，供可选适配器用于构建交易封装。",
    },
  }}
/>

### 各协议行为

<Tabs
  items={[
    "Lido (EVM)",
    "Eigenlayer (EVM)",
    "Jito (Solana)",
    "Marinade (Solana)",
  ]}
>
  <Tab value="Lido (EVM)">
    - 省略 `protocol` 时 EVM 链的默认协议 - 取消质押：通过 Lido WithdrawalQueue
    创建提款请求 - 代币标识符：`"stETH"`（或使用默认值）- 原生执行路径：批准
    stETH 到 WithdrawalQueue，然后调用 `requestWithdrawals` -
    返回交易哈希（`txid`）、提款请求 ID（`request_ids`）和下一步操作 -
    **认领功能尚未实现** — 通过 Lido UI 或链上读取监控提款可认领状态 - 支持
    Ethereum 主网
  </Tab>
  <Tab value="Eigenlayer (EVM)">
    - 显式设置 `protocol: "eigenlayer"` - 通过交易封装回退执行 -
    需要智能体或适配器提供封装字段（`to`、`data`、`value_wei`）
  </Tab>
  <Tab value="Jito (Solana)">
    - 省略 `protocol` 时 Solana 的默认协议 - 取消质押：通过 Jupiter 兑换将
    JitoSOL 兑换回原生 SOL - 代币标识符：`"JitoSOL"`（或使用默认值）-
    原生执行路径：在内部使用 Jupiter 兑换聚合器 - `slippage_bps`
    控制兑换滑点容忍度 - 返回签名和美元价值 - 支持 Solana 主网
  </Tab>
  <Tab value="Marinade (Solana)">
    - 显式设置 `protocol: "marinade"` - 通过交易封装回退执行 -
    需要智能体或适配器提供封装字段（`tx_b64`、`allowed_program_ids`）
  </Tab>
</Tabs>

### 响应

```json
{
  "chain": "ethereum",
  "protocol": "lido",
  "status": "pending",
  "txid": "0xabc...txhash",
  "request_ids": ["123456"],
  "next_action": "Monitor Lido WithdrawalQueue claimability in the Lido UI or via on-chain reads; Seashail claim support is not implemented yet.",
  "usd_value": 42.5
}
```

响应结构仅供参考，实际字段可能因协议而异。Lido 返回额外字段（`status`、`request_ids`、`next_action`）。Jito 返回 `signature` 而非 `txid`。

### 示例

<Tabs items={["Lido (Ethereum)", "Jito (Solana)"]}>
  <Tab value="Lido (Ethereum)">
    ```json
    {
      "chain": "ethereum",
      "protocol": "lido",
      "amount": "0.5",
      "amount_units": "ui"
    }
    ```

    注意：Lido 取消质押时 `token` 默认为 `"stETH"`。

  </Tab>
  <Tab value="Jito (Solana)">
    ```json
    {
      "chain": "solana",
      "protocol": "jito",
      "amount": "2.0",
      "amount_units": "ui",
      "slippage_bps": 100
    }
    ```

    注意：Jito 取消质押时 `token` 默认为 `"JitoSOL"`。

  </Tab>
</Tabs>

### 注意事项

- 需要[策略审批](/zh/docs/reference/tools-policy)——交易可能被自动批准、需要确认或被阻止，取决于策略配置和美元价值。
- **协议自动选择：** 若省略 `protocol`，Seashail 按链选择（EVM → Lido，Solana → Jito）。
- **Jito 兑换机制：** Solana 上的 Jito 取消质押使用 Jupiter 兑换将 JitoSOL 转换回 SOL。`slippage_bps` 控制此兑换的最大滑点容忍度。
- **Lido 提款限制：** Lido 取消质押异步创建提款请求。响应包含用于追踪的 `request_ids`，但 Seashail 尚未实现认领功能。通过 Lido UI 或链上读取监控提款可认领状态。
- **原生执行路径：** 提供 `amount` 和 `chain` 时，Lido（Ethereum stETH）和 Jito（Solana JitoSOL）有原生执行路径。其他协议回退到交易封装模式。
- **交易封装回退：** 如果原生执行不可用或提供了封装字段，Seashail 执行提供的交易封装。
- 另见：[DeFi 指南](/zh/docs/guides/defi)
