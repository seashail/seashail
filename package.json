{
  "name": "seashail",
  "private": true,
  "workspaces": [
    "apps/*",
    "crates/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "check": "turbo lint && turbo format:check && turbo check-types && bun run check:zh-copy",
    "check:zh-copy": "node scripts/check-zh-copy.mjs",
    "lint": "turbo lint",
    "lint:oxlint": "oxlint",
    "check-types": "turbo check-types",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "oxfmt": "latest",
    "oxlint": "latest",
    "oxlint-tsgolint": "latest",
    "turbo": "^2.8.6",
    "typescript": "^6.0.2",
    "ultracite": "^7.1.4"
  },
  "packageManager": "bun@1.3.3"
}
